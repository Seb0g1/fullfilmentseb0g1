# Инструкция по настройке чата с менеджером

## 📋 Обзор

Чат с менеджером позволяет пользователям сайта общаться с менеджерами через Telegram. Сообщения отправляются в Telegram группу, а ответы менеджеров приходят обратно на сайт.

## 🚀 Быстрая настройка

### 1. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather):
   - Отправьте `/newbot`
   - Придумайте имя и username для бота
   - Скопируйте токен бота

2. Создайте группу в Telegram:
   - Создайте новую группу
   - Добавьте бота в группу
   - Сделайте бота администратором (рекомендуется)

3. Получите ID группы:
   - Отправьте любое сообщение в группу
   - Перейдите по ссылке (замените `<ВАШ_ТОКЕН>`):
     ```
     https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
     ```
   - Найдите `"chat":{"id":-123456789}` - это ID группы (отрицательное число)

### 2. Настройка Backend сервера

1. Перейдите в папку `server`:
```bash
cd server
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в папке `server`:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
TELEGRAM_GROUP_CHAT_ID=-123456789
PORT=3001
```

4. Запустите сервер:
```bash
npm run dev
```

Сервер будет работать на `http://localhost:3001`

### 3. Настройка Frontend

1. Создайте файл `.env` в корне проекта (если его еще нет):
```env
VITE_API_URL=http://localhost:3001
```

2. Перезапустите dev-сервер:
```bash
npm run dev
```

## 📱 Как это работает

1. **Пользователь отправляет сообщение:**
   - Пользователь пишет сообщение в чате на сайте
   - Сообщение отправляется на backend сервер
   - Сервер отправляет сообщение в Telegram группу с указанием ID пользователя

2. **Менеджер отвечает:**
   - Менеджер видит сообщение в Telegram группе
   - Менеджер отвечает на сообщение (reply) в группе
   - Backend сервер отслеживает ответы и связывает их с пользователем

3. **Пользователь получает ответ:**
   - Frontend периодически запрашивает новые сообщения
   - Ответ менеджера появляется в чате на сайте

## 🔧 Настройка для продакшена

### Backend

1. Используйте переменные окружения на сервере
2. Настройте CORS для вашего домена
3. Используйте HTTPS
4. Рассмотрите использование базы данных вместо памяти

### Frontend

1. Обновите `VITE_API_URL` на URL вашего продакшн сервера
2. Пересоберите проект:
```bash
npm run build
```

## ❓ Частые проблемы

### Сообщения не отправляются в Telegram

- Проверьте токен бота в `.env`
- Убедитесь, что бот добавлен в группу
- Проверьте ID группы (должен быть отрицательным числом)
- Проверьте логи сервера

### Ответы менеджеров не приходят на сайт

- Убедитесь, что менеджер отвечает на сообщение (reply), а не пишет новое
- Проверьте, что сервер запущен и работает
- Проверьте логи сервера

### CORS ошибки

- Убедитесь, что backend сервер настроен на правильный порт
- Проверьте `VITE_API_URL` в `.env`
- В продакшене настройте CORS для вашего домена

## 📝 Структура файлов

```
server/
  ├── server.js          # Backend сервер
  ├── package.json       # Зависимости сервера
  └── .env              # Настройки (создайте сами)

src/
  ├── components/
  │   └── ManagerChat.vue  # Компонент чата
  └── utils/
      └── chatService.js   # Сервис для работы с API
```

## 🔒 Безопасность

- Никогда не публикуйте токен бота в публичных репозиториях
- Используйте переменные окружения
- В продакшене используйте HTTPS
- Ограничьте доступ к API по IP или используйте аутентификацию

