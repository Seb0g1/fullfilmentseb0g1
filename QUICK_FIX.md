# ⚡ Быстрое решение: ERR_CONNECTION_REFUSED

## Проблема

Вы видите ошибки в консоли браузера:
```
Failed to load resource: net::ERR_CONNECTION_REFUSED
localhost:3001/api/chat/messages
```

## Решение (30 секунд)

### Windows:

**Вариант 1 - Автоматический:**
```cmd
start.bat
```
Скрипт автоматически запустит backend сервер.

**Вариант 2 - Только backend:**
```cmd
run-backend.bat
```

**Вариант 3 - Вручную:**
```cmd
cd server
npm install
node server.js
```

### Linux/Mac:

```bash
cd server
npm install
node server.js
```

## Что происходит?

- Frontend (сайт) работает на порту 3000 (или другом)
- Backend (API сервер) должен работать на порту 3001
- Если backend не запущен → ошибка `ERR_CONNECTION_REFUSED`

## После запуска

Вы должны увидеть в консоли backend:
```
🚀 Chat server running on http://localhost:3001
📱 Telegram bot: ✅ Configured
```

Теперь сайт сможет подключаться к API!

## Первая настройка

При первом запуске создайте `server/.env`:

```env
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_GROUP_CHAT_ID=ваш_chat_id
PORT=3001
```

## Для разработки

Нужно запустить **2 процесса**:

1. **Backend** (терминал 1):
   ```cmd
   run-backend.bat
   ```

2. **Frontend** (терминал 2):
   ```cmd
   npm run dev
   ```

## Проверка

Откройте: http://localhost:3001/health

Должен вернуться: `{"status":"ok"}`

---

📖 Подробная инструкция: `BACKEND_START.md`

