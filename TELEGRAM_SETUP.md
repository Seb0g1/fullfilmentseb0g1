# Инструкция по настройке Telegram бота

## 🚀 Быстрая настройка

### 1. Создание бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: `Syndicate Fulfillment Bot`)
4. Придумайте username для бота (должен заканчиваться на `bot`, например: `syndicate_fulfillment_bot`)
5. **Важно!** Скопируйте токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Получение Chat ID

#### Для личных сообщений (самый простой способ):

1. Найдите бота **@userinfobot** в Telegram
2. Отправьте ему любое сообщение
3. Бот вернет ваш ID (например: `123456789`)
4. Это и есть ваш Chat ID

#### Для группы:

1. Добавьте вашего бота в группу
2. Сделайте бота администратором (опционально, но рекомендуется)
3. Напишите любое сообщение в группе
4. Перейдите по ссылке в браузере (замените `<ВАШ_ТОКЕН>` на токен из шага 1):
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
5. Найдите в ответе `"chat":{"id":-123456789}` - это ID группы (отрицательное число)

#### Для канала:

1. Создайте публичный канал или используйте существующий
2. Добавьте бота как администратора канала
3. Отправьте любое сообщение в канал
4. Используйте тот же способ, что и для группы

### 3. Настройка переменных окружения

1. В корне проекта создайте файл `.env` (если его еще нет)
2. Добавьте в него следующие строки:

```env
VITE_TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
VITE_TELEGRAM_CHAT_ID=ваш_chat_id
```

**Пример:**
```env
VITE_TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
VITE_TELEGRAM_CHAT_ID=123456789
```

3. Сохраните файл

### 4. Перезапуск проекта

```bash
npm run dev
```

### 5. Проверка работы

1. Откройте сайт в браузере
2. Заполните форму (калькулятор или контактную форму)
3. Отправьте заявку
4. Проверьте Telegram - должно прийти сообщение с данными заявки

## ❓ Частые проблемы

### Бот не отвечает / Не приходят сообщения

1. **Проверьте токен бота:**
   - Убедитесь, что токен скопирован полностью
   - Токен должен начинаться с цифр и содержать двоеточие

2. **Проверьте Chat ID:**
   - Для личных сообщений - положительное число
   - Для группы - отрицательное число
   - Убедитесь, что вы написали боту или добавили его в группу

3. **Проверьте переменные окружения:**
   - Файл `.env` должен быть в корне проекта
   - После изменения `.env` нужно перезапустить dev-сервер
   - Убедитесь, что нет лишних пробелов в значениях

4. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Перейдите на вкладку Console
   - Проверьте наличие ошибок

### Ошибка "Telegram credentials not configured"

Это означает, что переменные окружения не загружены. Убедитесь, что:
- Файл `.env` существует в корне проекта
- Переменные названы правильно: `VITE_TELEGRAM_BOT_TOKEN` и `VITE_TELEGRAM_CHAT_ID`
- Dev-сервер перезапущен после создания/изменения `.env`

### Сообщения приходят, но неформатированные

Это нормально - сообщения отправляются в HTML формате. Telegram автоматически их форматирует.

## 🔒 Безопасность

⚠️ **Важно!** Никогда не публикуйте токен бота и Chat ID в публичных репозиториях!

- Файл `.env` уже добавлен в `.gitignore`
- Не добавляйте `.env` в систему контроля версий
- При работе с production используйте переменные окружения на сервере

## 📱 Дополнительные возможности

Вы можете настроить бота для:
- Автоматических ответов
- Форвардинга сообщений
- Сохранения данных в базу
- Интеграции с CRM системой

Для этого нужно настроить webhook или использовать серверную часть (backend).

