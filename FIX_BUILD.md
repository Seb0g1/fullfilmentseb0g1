# 🔧 Исправление ошибки сборки

## Проблема

```
error during build:
[vite:terser] terser not found. Since Vite v3, terser has become an optional dependency.
```

## Решение

### Вариант 1: Установить terser (рекомендуется)

На сервере выполните:

```bash
cd /var/www/sakoo.ru
npm install --save-dev terser
```

Или если используете скрипт деплоя:

```bash
# Скрипт автоматически установит все зависимости из package.json
./deploy-full.sh
```

### Вариант 2: Использовать esbuild (быстрее, но без drop_console)

Если не нужна функция удаления console.log, можно использовать встроенный esbuild:

Измените `vite.config.js`:

```js
build: {
  minify: 'esbuild', // вместо 'terser'
  // terserOptions удалить
}
```

## После исправления

Пересоберите проект:

```bash
npm run build
```

## Проверка

После успешной сборки должна появиться папка `dist/` с файлами.

