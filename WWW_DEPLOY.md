# 🚀 Деплой через скрипт из /www

## Быстрый старт

### Вариант 1: Прямой запуск (рекомендуется)

1. **Скопируйте скрипт на сервер:**
   ```bash
   # Создайте директорию /www (если нет)
   sudo mkdir -p /www
   
   # Скопируйте скрипт
   sudo wget https://raw.githubusercontent.com/Seb0g1/fullfilmentseb0g1/main/deploy-full.sh -O /www/deploy-full.sh
   
   # Или скопируйте вручную через scp/sftp
   ```

2. **Сделайте скрипт исполняемым:**
   ```bash
   sudo chmod +x /www/deploy-full.sh
   ```

3. **Запустите скрипт:**
   ```bash
   sudo /www/deploy-full.sh
   ```

### Вариант 2: Через www-deploy.sh

1. **Скопируйте оба скрипта:**
   ```bash
   sudo mkdir -p /www
   sudo wget https://raw.githubusercontent.com/Seb0g1/fullfilmentseb0g1/main/www-deploy.sh -O /www/www-deploy.sh
   sudo wget https://raw.githubusercontent.com/Seb0g1/fullfilmentseb0g1/main/deploy-full.sh -O /www/deploy-full.sh
   sudo chmod +x /www/*.sh
   ```

2. **Запустите:**
   ```bash
   sudo /www/www-deploy.sh
   ```

## Что делает скрипт автоматически:

✅ **Клонирует/обновляет код с GitHub**
- Если репозиторий уже есть - обновляет
- Если нет - клонирует новый

✅ **Устанавливает зависимости**
- Frontend (npm install)
- Backend (npm install в server/)

✅ **Собирает frontend**
- Запускает `npm run build`
- Создает папку `dist/`

✅ **Настраивает Nginx**
- Копирует конфигурацию
- Создает симлинк
- Проверяет и перезагружает nginx

✅ **Запускает backend через PM2**
- Устанавливает PM2 (если нет)
- Запускает сервер
- Настраивает автозапуск

✅ **Настраивает права доступа**
- Правильные права для www-data
- Защита .env файлов

## Структура после деплоя:

```
/var/www/sakoo.ru/
├── dist/                    # Собранный frontend
├── server/                  # Backend
│   ├── server.js
│   ├── .env                 # Ваши токены (создается автоматически)
│   └── node_modules/
├── nginx-sakoo.conf         # Конфигурация nginx
└── ecosystem.config.cjs     # PM2 конфигурация

/etc/nginx/sites-available/sakoo.ru  # Активированная конфигурация
```

## Первый запуск:

После первого запуска скрипт создаст файл `server/.env` с шаблоном.
**Обязательно отредактируйте его:**

```bash
sudo nano /var/www/sakoo.ru/server/.env
```

Добавьте ваши токены:
```env
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_GROUP_CHAT_ID=ваш_chat_id
TELEGRAM_TOPIC_CHAT_CLIENT=id_топика
TELEGRAM_TOPIC_CALCULATOR=id_топика
TELEGRAM_TOPIC_CONTACT_FORM=id_топика
PORT=3001
NODE_ENV=production
```

После редактирования перезапустите backend:
```bash
pm2 restart fulfillment-backend
```

## Обновление сайта:

Просто запустите скрипт снова:
```bash
sudo /www/deploy-full.sh
```

Скрипт автоматически:
- Обновит код с GitHub
- Пересоберет frontend
- Перезапустит backend

## Проверка работы:

```bash
# Проверка backend
curl http://localhost:3001/health

# Проверка через nginx
curl http://sakoo.ru/health
curl http://sakoo.ru/api/health

# Статус PM2
pm2 status
pm2 logs fulfillment-backend
```

## Устранение проблем:

### Скрипт не запускается:
```bash
# Проверьте права
ls -la /www/deploy-full.sh
sudo chmod +x /www/deploy-full.sh

# Проверьте синтаксис
bash -n /www/deploy-full.sh
```

### Ошибка при клонировании:
- Проверьте доступ к GitHub
- Убедитесь, что git установлен: `sudo apt-get install git`

### Ошибка Nginx:
```bash
# Проверьте конфигурацию
sudo nginx -t

# Посмотрите логи
sudo tail -f /var/log/nginx/error.log
```

### Backend не запускается:
```bash
# Проверьте .env файл
cat /var/www/sakoo.ru/server/.env

# Проверьте логи PM2
pm2 logs fulfillment-backend

# Проверьте, что порт свободен
sudo netstat -tulpn | grep 3001
```

## Автоматизация (опционально):

Можно настроить cron для автоматического обновления:

```bash
# Редактируйте crontab
sudo crontab -e

# Добавьте строку (обновление каждый день в 3:00)
0 3 * * * /www/deploy-full.sh >> /var/log/fulfillment-deploy.log 2>&1
```

## Безопасность:

1. **Не коммитьте .env файлы!** Они уже в .gitignore
2. **Проверьте права:**
   ```bash
   sudo chmod 600 /var/www/sakoo.ru/server/.env
   ```
3. **Настройте firewall:**
   ```bash
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   ```
4. **Настройте SSL:**
   ```bash
   sudo certbot --nginx -d sakoo.ru -d www.sakoo.ru
   ```

